import { MAPS } from "./MAPS/maps.db";
import { PIECES } from "./PIECES/pieces.db";
import { TILES } from "./tiles.db";

const DB = {
  PIECES: PIECES,
  TILES: TILES,
  MAPS: MAPS
}

export default DB;
